export interface CommunicationItem {
  id: string;
  name: string;
  emoji: string;
  photoUrl?: string;
}

export interface CommunicationCategory {
  key: string;
  label: string;
  emoji: string;
}
